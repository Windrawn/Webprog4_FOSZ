<!DOCTYPE html>
<html>
	<head>
		<title>Macskás blog - Fajták</title>
		<% include ../../common/head %>
	</head>
	<body>
		
		<% include ../../common/header %>
		
		<div class="container">
			<div class="szovegdoboz">
				<h1 id="black">Fajták:</h1>
				<br>
				<% if (login) { %>
				<a class="btn btn-primary" href="/bejegyzesedit/new">Új bejegyzés</a>
				<% } %>
			</div>
			<div class="szovegdoboz">
				<hr>
				<% for(var i=0; i<bejegyzesek.length;i++){
				if (typeof !user){
				$enyem=false
				} else{
				$enyem=(user.username==bejegyzesek[i].username)
				}
				$first=(bejegyzesek[0]==bejegyzesek[i])
				if ($enyem) { %>
				
				<% if ($first) { %>
				<div class="row">
					<div class="col-md-5">
						<img class="img-fluid rounded mb-3 mb-md-0" src="<%= bejegyzesek[i].kep %>" alt="">
					</div>
					<div class="col-md-7">
						<h3><%= bejegyzesek[i].cim %></h3>
						<p>Feltöltő: <%= bejegyzesek[i].username %></p>
						<div class="btn-group" role="group" aria-label="Basic example">
							<a class="btn btn-primary" href="/bejegyzes/<%= bejegyzesek[i]._id %>">Tovább</a>
							<a class="btn btn-secondary" href="/bejegyzesedit/edit/<%= bejegyzesek[i]._id %>">Szerkesztés</a>
						</div>
					</div>
				</div>
				<hr>
				<% }else { %>
				<div class="row">
					<div class="col-md-5">
						<img class="img-fluid rounded mb-3 mb-md-0" src="<%= bejegyzesek[i].kep %>" alt="">
					</div>
					<div class="col-md-7">
						<h3><%= bejegyzesek[i].cim %></h3>
						<h4>Típus: <%= bejegyzesek[i].tartalom %></h4>
						<p>Feltöltő: <%= bejegyzesek[i].username %></p>
						<a class="btn btn-secondary" href="#">Szerkesztés</a>
					</div>
				</div>
				<hr>
				<% } %>
				
				<% }else { %>
				
				<% if ($first) { %>
				<div class="row">
					<div class="col-md-5">
						<img class="img-fluid rounded mb-3 mb-md-0" src="<%= bejegyzesek[i].kep %>" alt="">
					</div>
					<div class="col-md-7">
						<h3><%= bejegyzesek[i].cim %></h3>
						<p>Feltöltő: <%= bejegyzesek[i].username %></p>
						<a class="btn btn-primary" href="/bejegyzes/<%= bejegyzesek[i]._id %>">Tovább</a>
					</div>
				</div>
				<hr>
				<% }else { %>
				<div class="row">
					<div class="col-md-5">
						<img class="img-fluid rounded mb-3 mb-md-0" src="<%= bejegyzesek[i].kep %>" alt="">
					</div>
					<div class="col-md-7">
						<h3><%= bejegyzesek[i].cim %></h3>
						<h4>Típus: <%= bejegyzesek[i].tartalom %></h4>
						<p>Feltöltő: <%= bejegyzesek[i].username %></p>
					</div>
				</div>
				<hr>
				<% } %>
				
				<% } %>
				<% } %>
				<% if (bejegyzesek.length == 0) { %>
				<h2>Sorry, nincs bejegyzés ebben a témában</h2>
				<hr>
				<% } %>
				
			</div>
			<div class="szovegdoboz">
				<div class="btn-group" role="group" aria-label="Basic example">
					<a class="btn btn-info" href="">Az oldal tetejére</a>
					<a class="btn btn-info" href="/tema">Vissza</a>
					<a class="btn btn-info" href="/">Vissza a kezdőoldalra</a>
				</div>
			</div>
		</div>

		<% include ../../common/footer %>
		<% include ../../common/script %>
		
	</body>
</html>